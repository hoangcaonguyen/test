let n = prompt("nhập số n: ");
let i = prompt("nhập inputNumber: ");


function findOppositeNumber(n, i) {
    if (n <= 4 || n >= 20) {
        console.log("n phải nằm trong khoảng từ 4 đến 20");
    }
}

findOppositeNumber(n, i);